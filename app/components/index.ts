// Header Components
export { default as Header } from './header';
export { ScrollProgress } from './ui/scroll-progress';

// Hooks
export { useHeader } from '../hooks/use-header';

// Types
export type { MenuItem, HeaderState, HeaderActions, HeaderHook } from '../types/header'; 